version: "3"

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    environment:
      TZ: "Asia/Seoul"
    volumes:
      - /config/nginx:/etc/nginx/conf.d
    restart: always

  app:
    build: .
    container_name: "devook-ML"
    ports:
      - "8000:8000"
    environment:
      TZ: "Asia/Seoul"

